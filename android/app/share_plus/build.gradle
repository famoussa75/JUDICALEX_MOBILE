plugins {
    id "com.android.library"
    id "kotlin-android"
}

android {
    namespace "com.example.share_plus" // Namespace pour le module share_plus
    compileSdk = flutter.compileSdkVersion

    defaultConfig {
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
    }
}
